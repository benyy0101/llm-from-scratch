---
tags: [tool, application, agents, research]
aliases: ["OpenHands", "OpenDevin", "SWE-agent", "ACI", "Agent-Computer Interface"]
---

# OpenHands와 SWE-agent

[[Aider]]·[[Cline]]이 사람과 대화하며 함께 작업하는 페어프로그래밍 도구라면, SWE-agent와 OpenHands는 **"깃허브 이슈 하나를 던지면 사람 개입 없이 끝까지 패치·테스트해서 돌려준다"**를 목표로 하는 연구 계보의 완전 자율 에이전트입니다. 둘 다 [[SWE-bench]] 벤치마크를 통해 성능을 검증하며 사실상 그 벤치마크와 함께 발전해 왔습니다.

## SWE-agent (Princeton, 2024) — ACI라는 아이디어

SWE-agent가 던진 핵심 통찰은 "에이전트 성능은 모델 크기뿐 아니라 **에이전트에게 어떤 인터페이스를 쥐여주는가**에도 크게 좌우된다"는 것입니다. 이를 **ACI(Agent-Computer Interface)**라고 부릅니다.

- 모델에게 원시 셸을 그냥 열어주는 대신, `find_file`·`search_dir`처럼 목적이 뚜렷한 커스텀 명령을 제공
- 파일 편집 명령은 줄 번호 기반이고 편집 직후 문법 검사를 돌려, 깨진 코드를 곧바로 되돌리도록 설계
- 결과적으로 같은 모델이라도 범용 셸 접근보다 이런 ACI를 쓸 때 SWE-bench 해결률이 크게 올라감

즉 "도구 정의를 얼마나 잘 설계하는가"가 [[함수 호출 (Tool Use)]]의 `description` 품질 문제와 같은 축의 이야기이되, 개별 함수 하나가 아니라 에이전트가 코드베이스와 상호작용하는 전체 인터페이스 수준으로 확장된 것입니다.

## OpenHands (구 OpenDevin, All Hands AI)

SWE-agent가 연구 프로토타입에 가깝다면 OpenHands는 그 아이디어를 **완결된 플랫폼**으로 확장한 오픈소스 프로젝트입니다.

- **샌드박스 실행 환경** — 모든 코드 실행이 격리된 Docker 컨테이너 안에서 이뤄져, 에이전트가 호스트 시스템에 직접 닿지 않음
- **이벤트 스트림 아키텍처** — 에이전트의 모든 행동(Action)과 그 결과(Observation)를 하나의 이벤트 로그로 기록. 재현·디버깅이 [[에이전트 상태 관리 (LangGraph)|LangGraph의 체크포인트]]와 비슷한 목적을 다른 방식(그래프가 아니라 로그)으로 달성
- **다중 도구** — 코드 편집기, 브라우저, 터미널을 각각 별도 Action 타입으로 제공해 SWE-agent보다 넓은 작업 범위(웹 리서치 포함)를 커버
- **모델 독립적** — 다양한 LLM 백엔드를 붙일 수 있어 자체 서빙 모델 검증에도 쓸 수 있음

## Aider·Cline과의 위치 차이

| | Aider / Cline | OpenHands / SWE-agent |
|---|---|---|
| 상호작용 방식 | 사람이 지켜보며 승인·수정 지시 | 이슈 텍스트만 주고 무인으로 완결 |
| 평가 방식 | 실사용 체감(설문·정성 평가) | [[SWE-bench]] 같은 정량 벤치마크로 해결률 측정 |
| 목적 | 매일 쓰는 페어프로그래밍 도구 | "어디까지 완전 자율화가 가능한가"의 연구 질문 |

## 관련
- [[Aider]] · [[Cline]] — 사람과 함께 작업하는 페어프로그래밍형 대안
- [[함수 호출 (Tool Use)]] — ACI가 확장하는 도구 정의 품질 문제의 원형
- [[에이전트 상태 관리 (LangGraph)]] — 이벤트 스트림/체크포인트로 실행을 재현 가능하게 만드는 같은 목적의 다른 구현
- [[SWE-bench]] — 이 두 에이전트가 서로를 비교하는 데 쓰이는 벤치마크

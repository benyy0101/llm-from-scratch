---
tags: [concept, llm-basics, training, alignment]
aliases: ["SFT", "Supervised Fine-Tuning", "Instruction Tuning", "지시문 튜닝"]
---

# SFT (Instruction Tuning)

[[사전학습(Pretraining)]]을 마친 모델은 "다음 토큰을 그럴듯하게 잇는" 능력은 있지만, 질문에 "질문답게" 반응하는 법은 모릅니다 — 프롬프트를 줘도 답을 이어가는 대신 비슷한 질문을 하나 더 만들어버리는 식입니다. SFT는 (지시문, 응답) 쌍으로 이루어진 데이터로 모델을 다시 학습시켜, "지시문이 오면 응답으로 반응한다"는 대화 패턴 자체를 가르치는 단계입니다.

## 데이터 포맷

실무에서 흔히 보는 세 가지 형식이 있습니다.

- **Alpaca 포맷** — `instruction`(지시문) / `input`(부가 입력) / `output`(모범 응답) 세 필드로 구성. Stanford Alpaca는 이런 예시 5.2만 개를 GPT 계열 모델로 생성해 약 600달러에 만들었습니다.
- **ShareGPT 포맷** — 한 번의 질문-답이 아니라 여러 턴의 대화를 통째로 담고, 각 발화에 system/human/assistant 같은 역할을 붙입니다.
- **ChatML 포맷** — `<|im_start|>`/`<|im_end|>` 같은 특수 [[토큰]]으로 역할과 발화 경계를 표시. Qwen 계열 등에서 채택.

## 손실 마스킹 — 지시문은 안 배우고 응답만 배운다

SFT는 [[사전학습(Pretraining)]]과 달리 시퀀스 전체에 대해 손실을 계산하지 않습니다. 지시문(프롬프트) 토큰에는 손실 마스크를 0으로, 응답 토큰에는 1로 둬서, 역전파로 흘러가는 기울기가 응답을 생성하는 부분에만 영향을 주게 만듭니다. 이렇게 하지 않으면 모델이 "지시문을 스스로 생성하는 법"까지 배워버려, 정작 필요한 "지시문에 응답하는 법"과 뒤섞여 학습이 흐트러집니다.

## 학습률과 데이터 규모가 사전학습과 다른 이유

| 항목 | 사전학습 | SFT |
|---|---|---|
| 학습률 | ~3×10⁻⁴ | ~2×10⁻⁵ (약 15배 낮음) |
| 에폭 | 1회 | 2~3회 |
| 데이터 규모 | 수조 토큰 | 수만 건(2.7만~5.2만 예시) |

학습률을 크게 낮추는 이유는 사전학습으로 이미 갖춘 지식을 SFT 과정에서 망가뜨리지 않기 위해서입니다. 데이터도 "많이"보다 "질 좋게"가 우선인데, 이는 모델이 이미 언어와 지식을 갖고 있어서 SFT가 새로운 사실을 주입하는 단계가 아니라 기존 지식을 대화 형식으로 재배열하는 단계이기 때문입니다 — 그래서 정제된 수만 건이 정제 안 된 수십만 건보다 낫다는 결과가 반복적으로 보고됩니다.

## 관련
- [[사전학습(Pretraining)]] — SFT가 시작점으로 삼는 이전 단계
- [[RLHF]] · [[DPO]] — SFT 다음에 이어지는 선호도 기반 정렬 단계
- [[토큰]] — 손실 마스킹이 실제로 적용되는 단위
